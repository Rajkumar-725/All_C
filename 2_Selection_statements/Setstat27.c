//program to count total number of notes in given amount.
#include<stdio.h>
int main()
{
    int n,fifty,hundred,five_hundred,thousand,two_thousand;
    printf("Enter amount to calculate notes");
    scanf("%d",&n);

    if(n%50==0)
    fifty
}
